export type Memo = {
  data: {
    memo: {
      id: number
      title: string
      body: string
      category_id: number
      category_name: string
      // tag_list: ['tags', 'normalized']
    }
  }
}
